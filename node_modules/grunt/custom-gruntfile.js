module.exports = function(grunt) {

  // Project configuration.
  grunt.initConfig({
    task3: {foo: {}},
    task4: {foo: {}},
  });

  grunt.registerTask('default', ['alias1', 'alias2']);
  grunt.registerTask('alias1', ['task1:a', 'task3:b']);
  grunt.registerTask('alias2', 'My Alias task.', ['task2:c', 'task4:d']);
  grunt.registerTask('task1', function(arg) {
    grunt.log.writeln(this.name + ', ' + arg);
  });
  grunt.registerTask('task2', 'My basic task.', function(arg) {
    grunt.log.writeln(this.name + ', ' + arg);
  });
  grunt.registerTask('task3', function(arg) {
    grunt.log.writeln(this.name + ', ' + arg);
  });
  grunt.registerTask('task4', 'My multi task.', function(arg) {
    grunt.log.writeln(this.name + ', ' + arg);
  });

};
